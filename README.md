# actions

Reusable GitHub composite actions for secolarelabs projects.

| Action | Description |
|---|---|
| [tailscale-ssh](./tailscale-ssh) | Join a Tailscale tailnet, SSH to a node, and optionally sync files or run a command |
